<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="javeriana.edu.co.mockups">

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <uses-feature
        android:name="android.hardware.camera"
        android:required="true" />

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/logom"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:theme="@style/AppTheme.ReverseTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".LogInActivity"
            android:theme="@style/AppTheme.ReverseTheme.NoActionBar" />
        <activity
            android:name=".InfoReseActivity"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".InfoAlojaActivity"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".FechaReservaActivity"
            android:theme="@style/AppTheme" />
        <activity android:name=".CrearCuentaActivity" />
        <activity android:name=".MiPerfilActivity" />
        <activity
            android:name=".MiCuentaActivity"
            android:label="Opciones"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".CrearAlojamientoActivity"
            android:label="@string/title_activity_crear_alojamiento"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Home"
            android:label="@string/title_activity_home"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".RutaActivity" />
        <activity android:name=".HistoricoAlojamientoActivity" />
        <activity android:name=".AnfitrionAlojamientosActivity" />
        <activity android:name=".ReservasAnfitrionActivity" />
        <activity android:name=".BiografiaAnfitrionActivity" />
        <activity android:name=".BuscarAlojamientoActivity" />
        <activity android:name=".HistorialUsuarioActivity" />
        <activity android:name=".ReservasUsuariosActivity" />
        <activity android:name=".RutasUsuarioActivity" />
        <activity android:name=".CalificacionesActivity" />
        <activity android:name=".ModificarAlojamientoActivity"
            tools:ignore="DuplicateActivity" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".AlojamientoMapsActivity"
            android:label="@string/title_activity_alojamiento_maps" />
        <activity
            android:name=".ComoLlegarActivity"
            android:label="@string/title_activity_como_llegar" />
        <activity android:name=".CalificarActivity"></activity>
        <activity
            android:name=".AlojamientosEnCamino"
            android:theme="@style/AppTheme.NoActionBar"
            android:label="@string/title_activity_alojamientos_en_camino"></activity>
    </application>

</manifest>